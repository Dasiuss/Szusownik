# supabase/migrations/

Tu trafiają **migracje schematu** bazy (pliki `*.sql`), wersjonowane.

Supabase (przez integrację z GitHubem) aplikuje je automatycznie przy push na `main`.

Schemat do zaprojektowania: użytkownik, zjazdy, próbki/metadane (patrz `docs/koncepcja.md`,
punkt 6 otwartych punktów — docelowo szczegół implementacyjny).
